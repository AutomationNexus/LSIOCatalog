# LSIOCatalog
LSIO -> CognitiveSystems catalog generator (id: lsio). Scheduled CI converts linuxserver/docker-* readme-vars.yml into safe CS service definitions, validates each against a pinned CognitiveSystems renderer, and tags only validated output. CS's default 'lsio' catalog source points at a reviewed tag.
